<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PruebaNTT" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PruebaNTT" parent="Base.Theme.PruebaNTT" />
    <style name="EditTextStyle" parent="@android:style/Widget.EditText">
        <item name="colorControlNormal">@color/underline_gray</item>
        <item name="colorControlActivated">@color/black</item>
        <item name="colorControlHighlight">@color/black</item>
    </style>
    <style name="TextAppearance.App.Button.Medium" parent="TextAppearance.MaterialComponents.Button">
        <item name="android:textSize">16sp</item>
    </style>
    <style name="btn_primary" parent="Widget.MaterialComponents.Button">
        <item name="android:textAppearance">@style/TextAppearance.App.Button.Medium</item>
        <item name="rippleColor">@color/main_2</item>
        <item name="android:paddingLeft">40dp</item>
        <item name="android:paddingRight">40dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="backgroundTint">@color/main</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>